@model IEnumerable<QuanLyBanHang.Models.PhiShip>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChiPhi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChiPhi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PhiShipId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PhiShipId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PhiShipId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
